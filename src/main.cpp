/**
 * @Copyright Alexander Williams-Letondre 2024-12-28
 */

#include <stdint.h>
#include <stdio.h>
#include <cstdlib>

#include "main.hpp"


/**
 * @brief Entry point to the application. Declares nothing of worth all to itself.
 * 
 * @param[in] argc The number of arguments this program was called with. Default is 1.
 * @param[in] argv An array of pointers to character arrays (strings).
 * 
 * @return The status of the ran program.
 */
int main(int argc, char** argv) {
    printf("Hello, world!\n");
    return 0;
}
